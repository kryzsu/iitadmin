import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { JhiDataUtils } from 'ng-jhipster';

import { IArtifact } from 'app/shared/model/artifact.model';

@Component({
  selector: 'jhi-artifact-detail',
  templateUrl: './artifact-detail.component.html',
})
export class ArtifactDetailComponent implements OnInit {
  artifact: IArtifact | null = null;

  constructor(protected dataUtils: JhiDataUtils, protected activatedRoute: ActivatedRoute) {}

  ngOnInit(): void {
    this.activatedRoute.data.subscribe(({ artifact }) => (this.artifact = artifact));
  }

  byteSize(base64String: string): string {
    return this.dataUtils.byteSize(base64String);
  }

  openFile(contentType = '', base64String: string): void {
    this.dataUtils.openFile(contentType, base64String);
  }

  previousState(): void {
    window.history.back();
  }
}
